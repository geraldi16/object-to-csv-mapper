{
  "name": "object-to-csv",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env nodemon . --watch ./"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-register": "^6.26.0",
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.12"
  },
  "babel": {
    "presets": [
      ["env",
      {
        "targets": {
          "node": "current"
        }
      }]
    ]
  }
}
